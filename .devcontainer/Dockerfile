FROM calaos-os-builder:latest

# Install stuff here
RUN sudo pacman -Syu --noconfirm
RUN sudo pacman -Syu --noconfirm libsigc++ jansson luajit libusb libuv \
                            curl sqlite mosquitto imagemagick lua-socket pkgconfig \
                            libtool autoconf automake git gdb